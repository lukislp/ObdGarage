# Weekly Dependabot updates: GitHub Actions, NuGet (grouped) and the Docker base images. MAUI packages follow the workload, not Dependabot.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "fix(ci)"
    reviewers:
      - "lukislp"
  - package-ecosystem: nuget
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      dotnet:
        patterns: ["Microsoft.*", "System.*", "Npgsql.*"]
    ignore:
      # MAUI packages must match the installed workload; bump them together with the SDK.
      - dependency-name: "Microsoft.Maui.*"
      - dependency-name: "Microsoft.AspNetCore.Components.WebView.Maui"
    commit-message:
      prefix: "fix(deps)"
  # docker/server.Dockerfile and docker/web.Dockerfile: base-image digest bumps arrive as PRs like
  # in the sibling repos; a base-image MAJOR bump (new .NET line) is a deliberate upgrade instead.
    reviewers:
      - "lukislp"
  - package-ecosystem: docker
    directory: /docker
    schedule:
      interval: weekly
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    commit-message:
      prefix: "fix(docker)"
    reviewers:
      - "lukislp"
